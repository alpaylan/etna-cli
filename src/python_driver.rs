use crate::{config::ExperimentConfig, experiment::ExperimentSnapshot};

pub(crate) fn run_experiment(
    _experiment_config: &ExperimentConfig,
    _snapshot: ExperimentSnapshot,
) -> anyhow::Result<()> {
    todo!()
}
